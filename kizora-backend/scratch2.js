const gogo = require('node-gogoanime-scraper'); async function test() { try { const res = await gogo.searchAnime('One Piece'); console.log('Search result:', res[0]); if (res[0]) { const eps = await gogo.getAnime(res[0].id); console.log('Episodes length:', eps.episodes.length); const video = await gogo.getVideo(eps.episodes[0].id); console.log('Video:', video); } } catch (e) { console.error(e.message); } } test();
